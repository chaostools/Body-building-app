<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.curioustools.bodybuilder">


    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App">

        <activity android:name=".screens.start_exer.StartExerciseActivity" />
        <activity android:name=".screens.aboutme.AboutMeActivity" />
        <activity android:name=".screens.challangelist.ChallengesActivity" />
        <activity android:name=".screens.dashboard.DashBoardActivity" />


        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>


</manifest>